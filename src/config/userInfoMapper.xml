<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userInfoMapper">

    <insert id="register" parameterType="map">
        INSERT INTO USERINFO (
            bid,
            name,
            phone,
            email,
            storeName,
            storeType,
            storeCategory,
            pw,
            signDate,
            agreementDate,
            marketingDate
        )
        VALUES (
            #{bId},
            #{name},
            #{phone},
            #{email},
            #{storeName},
            #{storeType},
            #{storeCategory},
            #{pw},
            SYSDATE,
            SYSDATE,
            <choose>
                <when test="marketingDate != null and marketingDate != ''">
                    SYSDATE
                </when>
                <otherwise>
                    NULL
                </otherwise>
            </choose>
        )
    </insert>
    
    <select id="checkMemberByVO" resultType="com.kostaErp.model.VO.userInfoVO">
    	SELECT bId, name, storeName, storeType, pw FROM USERINFO 
		WHERE bId = #{bId} AND name = #{name} AND pw = #{pw}
    </select>
    
    <update id="setPw" parameterType="com.kostaErp.model.VO.userInfoVO">
    	UPDATE USERINFO SET pw = #{pw} WHERE bId = #{bId} AND NAME = #{name} AND PHONE = #{phone}
    </update>
    
    <select id="login" parameterType="map" resultType="com.kostaErp.model.VO.userInfoVO">
    SELECT bId, name
    FROM USERINFO
    WHERE bId = #{bId}
    AND pw = #{pw}
</select>

</mapper>